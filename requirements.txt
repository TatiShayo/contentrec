# Core web stack. Version floors chosen to exclude known advisories flagged by
# pip-audit (2026-07): starlette < 1.3.1 (PYSEC-2026-161/248/249 DoS),
# torch < 2.13.0 (PYSEC-2025-194). Keep these floors when upgrading.
fastapi>=0.115.0
starlette>=1.3.1
uvicorn>=0.30.0
pydantic>=2.7.0

# Numerics / storage
numpy>=1.26.0
scipy>=1.11.0

# ML / embeddings (optional-heavy; the engine degrades gracefully if absent)
lightfm            # Linux/Docker only — Cython build fails on Windows
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
torch>=2.13.0

# HTTP client used by tests / TestClient
httpx>=0.27.0

# Testing
pytest>=8.0.0

# Security auditing (dev): run `python -m pip_audit`
pip-audit>=2.7.0
